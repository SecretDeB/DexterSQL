# ── dep_tree method ──────────────────────────────────────────────
spacy>=3.7,<4          # dependency parsing  (then: python -m spacy download en_core_web_sm)
sqlglot>=25.0          # SQL AST validation

# ── LLM access ───────────────────────────────────────────────────
openai>=1.40           # OpenAI-compatible chat completions
httpx>=0.27
tenacity>=8.0          # retry/backoff
tiktoken>=0.7          # prompt-length accounting

# ── schema linking (FAISS + MinHash-LSH indexes) ─────────────────
faiss-cpu>=1.7
datasketch>=1.6
numpy>=1.24

# ── value retrieval (embeddings) ─────────────────────────────────
sentence-transformers>=3.0
torch>=2.0
transformers>=4.40
chromadb>=0.5          # optional: only for build_backend = "chroma"

# ── config / data ────────────────────────────────────────────────
pydantic>=2.0
toml>=0.10
tqdm>=4.66
loguru>=0.7
pandas>=2.0             # execution/schema result handling
tabulate>=0.9           # execution/schema result formatting
chardet>=5.0            # DB text encoding detection
